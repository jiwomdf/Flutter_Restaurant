name: fundamental_beginner_restourant
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  cached_network_image: ^3.3.1
  equatable: ^2.0.5
  either_dart: ^1.0.0
  provider: ^6.0.3
  http: ^0.13.3
  sqflite: ^2.3.1
  android_alarm_manager_plus: ^2.0.3
  flutter_local_notifications: ^9.7.0
  rxdart: ^0.27.5
  intl: ^0.17.0
  shared_preferences: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/local_restaurant.json